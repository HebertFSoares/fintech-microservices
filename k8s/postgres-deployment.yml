apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: fintech
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: accountdb
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: fintech-secret
                  key: DB_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: fintech-secret
                  key: DB_PASS

---
apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: fintech
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432